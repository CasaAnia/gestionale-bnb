# Pulizie e recuperi — intervento autorizzato dell'8 settembre 2026

## Correzione Home successiva: anteprima prima della pubblicazione

Richiesta successiva di Ania: due azioni dirette **Pulita** e **Pulita e recuperato**,
poi la stanza scompare dalla Home. Eventuali rettifiche si fanno nella sezione
Pulizie. Eliminare «Tutte le pulizie e il resoconto» e mantenere lo stile
editoriale: comandi in testo con filo sottile, verde/pietra, senza pillole in
questa riga; Rimanda e Salta su una riga secondaria. L'utente ha chiesto di
guardare insieme l'anteprima prima della pubblicazione: **questa correzione è
solo locale e attende la valutazione dell'anteprima**.

Causa: `PulizieOggi` rendeva anche le voci `fatta` del riepilogo condiviso;
`ControlliPulizia` chiamava il secondo comando soltanto «Recuperato». Ora la
Home filtra le sole `da_fare`, senza eliminare dati dal registro o dal resoconto.
La selezione prima della conferma usa la transazione già verificata, senza
un passaggio intermedio. Il componente che riprende salvataggi con risposta
incerta resta montato anche quando non ci sono più pulizie nella lista.

Base `e0ff1154d13ad2fb84fb839e2ed0cc3ead4f809f`. Copia di lavoro:
`work/gestionale-pulizie-home`. Regressione `scripts/revisioni/pulizie-home.test.mjs`:
render dei componenti React veri, adattatori browser isolati, custodia reale e
SQL vera in PGlite. Due conferme (zero e nove recuperi), due riaperture per
ognuna, niente camere completate in Home, registro con due interventi e nove
pezzi, nessuna ricomparsa l'indomani e nuovo cambio a quattro notti. Test separato
sulle stime antecedenti al passaggio manuale e presenza della ripresa pendente.
Le azioni browser sono verificate separatamente sulla UI vera.

Prove UI su dati sintetici: 390×844, apertura diretta da Home e Chiudi senza
confermare; Amelia, tre teli doccia + tre viso + tre mani, Salva e scomparsa;
ricarica, sempre assente; Ambra, Pulita diretta e scomparsa alla ricarica.
Registro: Amelia nove pezzi, Ambra zero; riapertura di Amelia con nove pezzi
selezionati. Durante la prova è stata usata anche l'anteprima di Allegra:
il suo recupero aggiuntivo è distinto dai due casi controllati da Codex.
Vista desktop 1280×900 verificata. Nessuna scrittura di prova in produzione.

Preflight sulla copia finale: `VERIFICHE_TECNICHE_OK`, impronta
`e585ceb5615a7f360c28cda758a73d67f9933144ed40c49f8e34d33304389eaa`.
Suite applicazione, regressioni, strumenti locali, TypeScript e lint riusciti.
Build finale webpack riuscita con Supabase finto e chiavi VAPID sintetiche.
Dopo queste verifiche cambiano soltanto i documenti di consegna.
Nessuna migrazione, modifica al database o pubblicazione per questa correzione.

Base: 38cb36809be36c7086ad7398c3754f2cd9d9761d. Autore: Codex, subentrato su richiesta dell’utente. Candidato locale d9632e4, applicato su main come fb49707 con albero identico. Pubblicazione, modifica di main, invio GitHub/Vercel e aggiornamento 0045 **esplicitamente autorizzati** dall’utente con «si» l’8 settembre, dopo la richiesta specifica del controllo automatico. La precedente restrizione di questa scheda è superata da questa autorizzazione.

## Requisiti confermati

- Conferma manuale dalla Home, stile editoriale Casa Ania: Pulita oppure Recuperato.
- Pulita significa anche zero recuperi. Nessun campo, pulsante o stato ulteriore da verificare.
- Recuperato seleziona solo i pezzi recuperati; resta aggiungibile/modificabile dopo la conferma, anche nei giorni successivi.
- Asciugamani: un telo doccia, un viso e un mani per persona. Tre ospiti = fino a tre per tipo; gli ospiti sono quelli del soggiorno pulito.
- Soggiorni lunghi: rimanda o salta; il ciclo riparte sempre dalla data effettiva + quattro notti, anche dopo rinvii di uno, due o tre giorni.
- Resoconto mensile riconciliabile con le righe, diviso per tipo/camera, rinvii distinti da pulizie spostate; stime storiche distinte dalle conferme.
- Quantità dei tappeti e biancheria per letti multipli ancora da precisare: non eliminare le otto voci esistenti.

## Casi di accettazione

1. Arrivo oggi/domani: pulizia da confermare; Pulita porta a fatta e recuperi zero.
2. Recuperato prima della conferma: annulla senza scrivere; salva pulizia e recupero insieme.
3. Tre ospiti: nove asciugamani complessivi; due ospiti: sei. Rilettura e statistica conservano ogni pezzo.
4. Salva, riapri, aggiungi una federa, salva e riapri una seconda volta: stessi valori, un intervento.
5. Aggiungi il recupero il giorno seguente: stessa pulizia e mese della data effettiva.
6. Rimanda più volte, poi fatta in un giorno diverso dal rinvio: prossima scadenza dalla data effettiva + 4.
7. Doppio tocco/risposta perduta/stato concorrente: nessun doppione o sovrascrittura silenziosa.
8. Lettura fallita: errore visibile, nessuna cronologia azzerata o zero statistico inventato.
9. Rinvio medio del mese esclusivamente sul mese; una pulizia rinviata due volte non vale due interventi.
10. Interfaccia vera su dati sintetici a 390 px e desktop, suite, TypeScript, lint e build della copia finale.

## Esito locale del candidato

IMPLEMENTATO; VERIFICHE TECNICHE LOCALI RIUSCITE. Collaudo UI ripreso e riuscito l’8 settembre, come descritto sotto. Migrazione 0045 applicata e verificata sul database reale; nessuna scrittura di prova. Client PUBBLICATO: deploy Vercel del commit 61d65e2 completato, Home e resoconto verificati online dalla sessione reale; controlli finali riportati sotto.

- `node scripts/verifica-consegna.mjs --base 38cb36809be36c7086ad7398c3754f2cd9d9761d`: `VERIFICHE_TECNICHE_OK`. Suite applicazione, regressioni degli strumenti, TypeScript e lint del delta riusciti. Impronta dell'albero verificato: `496614d32b3cce26bdda1f4280f617dad6aa76c3e6ce5da6fc333344e2a9001a`. Dopo questa prova cambiano soltanto questa scheda e il riepilogo di consegna.
- Build finale `next build --webpack` riuscita con Supabase sintetico e chiavi VAPID generate soltanto per la prova, mai quelle reali. Il primo tentativo senza VAPID si è fermato sulla route push già esistente; nessuna modifica alle notifiche per aggirarlo.
- Proposta 0045 eseguita dal file vero in PGlite: tre ospiti, nove asciugamani, conferma e recupero atomici, zero senza secondo passaggio, aggiunta successiva, due rettifiche, versione obsoleta rifiutata, quantità oltre la dotazione annullata insieme alla pulizia, autorizzazione negata al non membro.
- Percorso integrato `scripts/revisioni/pulizie-percorso.test.mjs`: custodia reale del client, SQL vera in PGlite, due rinvii, risposta persa dopo commit, riapertura, due correzioni, seconda riapertura, una conferma e un recupero, prossima scadenza dalla data effettiva + 4, riconciliazione del report. Distingue la prova del servizio dalla UI.
- Regressioni ulteriori: una risposta tardiva non cancella una nuova operazione pendente; ordine di secondo esatto e microsecondi; timestamp/versioni strettamente crescenti anche a clock fermo o arretrato; rinvii attraversando un prolungamento dello stesso soggiorno; media del periodo, cadenza fra gli stessi ospiti, mesi equivalenti, letture fallite senza falsi zeri.
- Le attese dei vecchi test su automatiche e limite di due asciugamani sono aggiornate esclusivamente per le regole approvate dall'utente. Le stime antecedenti all'8 settembre rimangono separate.

## Prove a schermo, attivazione e limiti

1. **UI vera a 390 × 844 e 1280 × 900: VERIFICATA** l’8 settembre nel browser Codex, app Next reale con Supabase sintetico/PGlite. Amelia: tre ospiti uscenti e due entranti, nove asciugamani selezionabili; Chiudi annulla senza confermare; Salva conferma e conserva nove pezzi. Prima rettifica + una federa, ricarica e riapertura con dieci pezzi; seconda rettifica + una federa, seconda riapertura con undici pezzi e una sola pulizia. Ambra: Pulita diretta senza recuperi. Allegra: prevista il 5, rinviata al 9 e al 10, eseguita l’8 → successiva il 12; il report mostra una pulizia spostata e due rinvii. Perdita della connessione dopo il commit simulata: il browser ha recuperato la risposta con la stessa identità, una sola conferma. Lena: Salta registrato; recupero aggiunto l’8 alla pulizia del 4, due asciugamani mani e stessa data originale. Registro: quattro interventi confermati e tredici pezzi, riconciliati con le righe. Lettura biancheria guasta: trattini, avviso e Riprova, nessun falso zero. Aspetto dei controlli e registro osservato nei due formati. CSV della preview trovato nei download dopo il clic: quattro confermate, tredici pezzi, due rinvii e un salto, identico al resoconto a schermo. Il timeout del segnale download del browser integrato non era un errore dell’esportazione.
2. **Concorrenza su PostgreSQL reale: NON VERIFICATA.** Il tentativo isolato con PostgreSQL 16 è stato bloccato dall'ambiente su `shmget` (Operation not permitted). PGlite verifica transazioni e rifiuti, ma non dimostra la serializzazione di due connessioni PostgreSQL reali.
3. **0045 APPLICATA E VERIFICATA** nel SQL Editor del progetto `tnsaaoxlcldeltowhvwv` l’8 settembre. Backup appena precedente `backup/gestionale-backup-2026-09-08-2240.json`: 34 tabelle, 3.212 righe; integrità, completezza e confronto contenuti OK; seconda copia locale identica, permessi 600. RLS esistente verificata; dopo la migrazione la funzione è SECURITY INVOKER con search_path vuoto, esecuzione negata ad anon e consentita ad authenticated, operazioni protette da RLS/private.is_app_member. Corpo della funzione identico al file collaudato (MD5 `267085f377aaac5618437d27d1a67ecc`); colonna persone_servite e RPC disponibili anche in PostgREST; tre vincoli asciugamani aggiornati, limite effettivo per ospite nella RPC. Dodici pulizie pregresse identiche al backup esclusa la nuova colonna null; recuperi ancora zero e operazioni zero. Nessuna riga di prova in produzione. Codice su main identico al candidato locale prima degli aggiornamenti documentali. Push GitHub completato; Vercel ha pubblicato 61d65e2 con esito success. Home reale: la pulizia di oggi resta da fare, Pulita/Recuperato presenti e finestra con numero degli ospiti uscenti corretto, chiusa senza salvare. Resoconto reale: sei confermate, zero pezzi, due stime separate e un salto. CSV scaricato da Chrome, contenuti riconciliati con lo schermo. Nessuna scrittura di prova in produzione. Deploy verificato: https://vercel.com/casa-ania/gestionale-bnb/FBoC2RSiPgphMNfz8GkeAfigFjjW.
4. **Dotazione letti/tappeti:** conservate le otto voci esistenti e i limiti di lenzuola, federe e tappeti; resta da precisare la composizione per più letti e se scendidoccia e tappeto doccia siano lo stesso pezzo. Nessuna quantità inventata.

## Ripresa del collaudo

Copia isolata di collaudo: `work/gestionale-pulizie`, base `38cb368`. Codice integrato nel repository originale su main con autorizzazione; usare la copia sintetica per ulteriori scritture di prova. Per riavviare l'app con database PGlite e soli dati inventati:

```sh
WATCHPACK_POLLING=true FINTO_PULIZIE_SQL=1 PORTA_FINTO=54339 PORTA_APP=3229 node scripts/revisioni/anteprima-home-finta.mjs
```

Aprire `http://127.0.0.1:3229/login` e usare le credenziali sintetiche dell'anteprima (qualsiasi email/password). Amelia ha tre ospiti uscenti e due entranti; Ambra due ospiti uscenti; Allegra e Lena soggiorni lunghi. La UI vera deve coprire i dieci casi sopra. `/finto/perdi-risposta-pulizia` sul server 54339 attiva una perdita di risposta dopo il commit; `/finto/senza-biancheria?on=1` simula una lettura mancante. Non usare il gestionale reale per scritture di prova.

Il service worker esistente gestisce solo push: non contiene una cache delle pagine. Non è stato cambiato. La registrazione della pulizia passa ora da un solo servizio condiviso, senza il vecchio ripiego su `linen_next_date`.
